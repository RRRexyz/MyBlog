---
date:
  created: 2025-03-24
  updated: 2025-03-24
authors:
  - Rexyz
categories:
  - 开发
tags:
  - ROS2
  - 机器人
---

# ROS2基本操作
使用Ubuntu24.04，安装ROS2 Jazzy，其基本操作命令与ROS1有显著区别，此帖记录ROS2主要命令。

<!-- more -->

!!! warning "注意"
    ROS2 命令支持Bash，Zsh，对Fish的适配很差，建议使用Bash或Zsh。

## 激活ROS2环境
需要在终端中运行
```bash
source /opt/ros/jazzy/setup.bash
```
为了方便，可以将上述命令写入`.bashrc`文件中，以后打开终端就自动激活ROS2环境。

## 启动ROS2节点
```bash
ros2 run package_name node_name
```

## 查看系统结构
```bash
rqt_graph
```

## 查看当前话题
```bash
ros2 topic list
```

## 查看当前服务
```bash
ros2 service list
```
如果要显示类型：
```bash
ros2 service list -t
```

## 发布话题
```bash
ros2 topic pub /topic_name topic_type "message"
```

限制发送一次：
```bash
ros2 topic pub --once <话题名称> <消息类型> '<消息内容>'
```

按固定频率发送消息：
```bash
ros2 topic pub --rate <频率> <话题名称> <消息类型> '<消息内容>'
```

示例：
```bash
ros2 topic pub /turtle1/cmd_vel geometry_msgs/msg/Twist "{linear: {x: 1.0}, angular: {z: 0.5}}" --once
```

## 输出话题
```bash
ros2 topic echo /topic_name
```

## 调用服务
ROS2必须显式指定服务类型，否则会报错。
```bash
ros2 service call /service_name service_type "request"
```
示例：
```bash
ros2 service call /spawn turtlesim/srv/Spawn "{x: 3.0, y: 3.0, theta: 0.0, name: 'turtle2'}"
```

## mimic输入输出重映射
在ROS2中，`mimic`节点的输入输出话题名称和参数传递方式与ROS1不同。ROS2的等效命令为：
```bash
ros2 run turtlesim mimic --ros-args --remap input/pose:=turtle1/pose --remap output/cmd_vel:=turtle2/cmd_vel
```

## 查看话题消息类型
```bash
ros2 topic type /topic_name
```

## 查看消息接口说明
ROS2 将消息（msg）、服务（srv）和动作（action）统一归类为接口，因此相关命令从 `rosmsg` 改为 `ros2 interface`。
```bash
ros2 interface show /msg_name
```

## 录制运动过程
```bash
ros2 bag record -a -o bag_name
```
其中`-a`参数表示记录所有话题，`-o`参数指定输出文件名。


